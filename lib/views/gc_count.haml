:javascript
  setTimeout(function() {
    window.location.reload();
  }, 1000);

%h3 ObjectSpace.count_objects
%table
  - objs = ObjectSpace.count_objects.to_a
  - objs.sort { |a, b| b[1] <=> a[1] }.each do |k, v|
    %tr
      %td
        = k
      %td
        = v
